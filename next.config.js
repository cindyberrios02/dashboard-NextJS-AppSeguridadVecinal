/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js automáticamente detecta la carpeta src
  experimental: {
    // Configuraciones experimentales si las necesitas
  },
}

module.exports = nextConfig;